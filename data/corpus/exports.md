# Reports and exports

Operators may create a redacted CSV export for cases assigned to their queue. The export removes email addresses, phone numbers, and free-form attachment text.

An export link expires after twenty-four hours. Downloading an export creates an audit event with the report identifier and operator identifier.

Raw attachments are never included in a standard export. Administrators must use the evidence workflow when a raw attachment is needed for an approved investigation.
